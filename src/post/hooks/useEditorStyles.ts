// Import the CSS styles directly
import '../components/PostTextEditor.styles.css';

/**
 * Hook to manage editor styles
 * Styles are loaded via import, so this is a no-op hook
 * Kept for consistency and potential future dynamic loading
 */
export function useEditorStyles() {
  // Styles are loaded via import statement above
  // This hook exists for API consistency and future extensibility
}