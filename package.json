{
  "name": "daily-writing-friends",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.19.0 <23"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm --filter web test",
    "test:run": "pnpm --filter web test:run",
    "type-check": "pnpm --filter web type-check",
    "db:types": "supabase gen types typescript --local --schema public > apps/web/src/shared/external/database.types.ts",
    "lint": "pnpm --filter web lint",
    "validate": "pnpm --filter web validate",
    "gate:check": "node visual-gate/check.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "lighthouse": "^13.3.0",
    "web-vitals": "^5.3.0"
  }
}
